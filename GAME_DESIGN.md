# Seal 当前玩法

## 核心循环

`Seal` 当前是一个最小化的中心防守增量原型。

- 玩家固定在竞技场中心。
- 敌人从四条边进入并向中心移动。
- 击杀敌人会增加分数。
- 敌人到达中心后消失，但不会提供分数。
- 敌人难度、商店和队伍规划暂不实现。

## 操作

- `移动鼠标`：用小圆点控制瞄准方向，玩家到场地边缘会显示一条长虚线射线。
- `鼠标左键点击战场`：朝鼠标方向发射一发子弹。
- `鼠标左键点击补弹按钮`：购买一次局内补弹。
- `Space`：切换到下一个角色。
- `Esc`：退出游戏。

点击战场时触发攻击，子弹朝玩家到鼠标的方向飞行，不会自动选择敌人。冷却未结束时不会发射。角色池为空时，
玩家使用间隔 `0.10` 秒、伤害 `10`、速度 `160` 的基础射击。基础子弹是半径 `2.5`
的白色实心圆，绘制形状和碰撞体保持一致。

玩家每局初始拥有 `30` 发子弹，每次成功发射消耗 `1` 发。子弹耗尽后不能继续攻击，
不会自动恢复或装填。玩家每局初始拥有 `3` 枚金币；点击右上角补弹按钮会消耗 `1`
枚金币并补充 `10` 发，每局最多购买 `3` 次。按钮使用白色子弹图标和“图标 / 费用”
表达购买信息。`Player:buy_ammo()` 是统一购买入口，后续可以继续接入其他
金币来源。

## 角色状态

当前具体角色池为空。通用 `Hero` 基类、切换框架和子弹系统继续保留，之后可以在不恢复旧设计的情况下，一个一个重新制作角色。

以下已删除的实现只作为技术参考保存在归档中：

- `docs/archive/ARC_IMPLEMENTATION.md`
- `docs/archive/AREA_EFFECTS_IMPLEMENTATION.md`

归档内容不属于当前角色池，也不能视为最终角色规则。
