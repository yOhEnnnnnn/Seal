# Seal 当前玩法

## 核心循环

`Seal` 当前是一个最小化的中心防守增量原型。

- 玩家固定在竞技场中心。
- 敌人从四条边进入并向中心移动。
- 击杀敌人会增加分数。
- 敌人死亡时有 `20%` 概率掉落 `1–2` 枚金币，每枚金币价值 `1`。
- 金币表现为半径 `2` 的黄色小圆粒子，死亡时向外轻微弹出，之后永久留在原地。
- 鼠标左键点击金币才会拾取；实际点击半径为 `6`，一次点击只拾取一枚金币。
- 拾取时金币在 `0.32` 秒内向上移动 `10` 像素，通过横向压缩模拟翻转一整圈，最后淡出。
- 敌人到达中心后消失，但不会提供分数。
- 敌人难度、商店和队伍规划暂不实现。

## 操作

- `Space`：切换到下一个角色。
- `Esc`：退出游戏。

攻击会在冷却结束后自动触发，目标由系统自动选择。范围内没有有效目标时，不会消耗本次攻击。角色池为空时，
玩家使用间隔 `0.6` 秒、伤害 `10`、速度 `160` 的基础射击。基础子弹是半径 `2.5`
的白色实心圆，绘制形状和碰撞体保持一致。

## 角色状态

当前具体角色池为空。通用 `Hero` 基类、切换框架和子弹系统继续保留，之后可以在不恢复旧设计的情况下，一个一个重新制作角色。

以下已删除的实现只作为技术参考保存在归档中：

- `docs/archive/ARC_IMPLEMENTATION.md`
- `docs/archive/AREA_EFFECTS_IMPLEMENTATION.md`

归档内容不属于当前角色池，也不能视为最终角色规则。
