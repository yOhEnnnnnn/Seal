# Seal 五角色轮换玩法重构草案

> 状态：评审稿，不代表最终实现
>
> 日期：2026-09-08
>
> 本文只规划玩法，不修改代码
>
> 核心约束：每局携带 5 名角色，但战场上始终只有 1 名当前角色

## 1. 一句话定义

`Seal` 是一款以五角色轮换和三选一商店为核心的低操作俯视角构筑游戏。

玩家在一局中携带 5 名角色。五人共享同一个玩家本体，场上只显示并启用当前角色，其余 4 人只在 HUD 中待命。玩家观察怪物组成，通过短按 `Q` 按队列顺序切换角色，用不同的自动攻击机制解决不同威胁。

核心判断循环为：

```text
识别主要威胁
→ 判断当前角色是否适合
→ 决定立即切换还是等待更好的时机
→ 用对应攻击机制处理威胁
→ 走位并准备下一次轮换
```

## 2. 已冻结的方向

以下内容作为本轮设计前提，不再使用“五人同时作战”的方案：

- 每局携带恰好 5 名不同角色。
- 战场上同一时间只有当前角色可见、可攻击、可触发角色机制。
- 其余 4 名角色只显示在 HUD 队列中，不生成战场单位，也不进行攻击。
- 五名角色共享玩家的位置、生命、护盾、碰撞体和穿梭能力。
- 短按 `Q` 单向切换到队列中的下一名角色，第 5 名之后回到第 1 名。
- 不加入五人围绕核心、楔形站位、成员跟随或任何队形玩法。
- 不区分“基础攻击”和“主印效果”；当前角色直接使用自己的完整攻击机制。
- 不设置候补伤害系数，也不让待命角色提供常驻输出。
- 现有射手与守卫只是原型角色，可以被新角色替换，不作为必须保留的职业。

## 3. 五角色轮换系统

### 3.1 阵容与队列

- 开局从角色候选中选择 5 名，不能重复。
- 选定后安排固定轮换顺序，例如：

```text
烬火 → 电弧 → 霜痕 → 游光 → 第五角色 → 烬火
```

- HUD 始终显示完整队列、当前角色、下一名角色和切换冷却。
- 当前角色放大并使用完整颜色，下一名角色增加方向提示，其余角色降低亮度。
- 波次进行中不能倒序切换，也不能直接选择某个角色。
- 每波结束后的商店阶段允许免费调整一次队列顺序。
- 调整顺序只改变接下来的切换路径，不改变角色已有的冷却和状态。

队列顺序本身是一项构筑决策。相邻角色应该能够覆盖彼此弱点，常用应对角色也不应被排在彼此相距过远的位置。

### 3.2 共享与独立状态

五名角色共享：

- 玩家位置和移动方向。
- 生命值、护盾和受击无敌时间。
- 碰撞体和穿梭冷却。
- 商店提供的全局属性与通用状态。

每名角色独立保存：

- 攻击冷却。
- 连击、蓄力、攻击次数等角色计数器。
- 由角色自身施加且明确标注的临时状态。

待命期间：

- 攻击冷却继续自然恢复。
- 角色不会攻击，也不会增加命中、击杀或连击计数。
- 需要持续命中维持的效果会按各自规则衰减。
- 已经发射的子弹和已经施加的敌人状态不会因切换而消失。

### 3.3 当前角色

- 当前角色使用自己的颜色、攻击范围、锁敌规则和完整攻击机制。
- 自动攻击仍是唯一常规攻击方式，不增加主动技能键。
- 默认锁定规则仍为“攻击范围内最近的合法目标”，角色可以用明确规则改变优先级。
- 切换角色只改变战斗能力，不改变玩家本体的移动速度、碰撞尺寸或生命上限。
- 所有角色都能对所有普通怪物造成有效伤害，克制关系只提高效率，不制造强制换人的免疫门槛。

## 4. Q 切换规则

### 4.1 保留当前操作

```text
移动鼠标：控制穿梭方向
鼠标左键：向目标方向穿梭
自动攻击：由当前角色执行
短按 Q：切换到下一名角色
Esc：暂停
```

- `Q` 只在按下瞬间触发一次，长按不会连续切换。
- 不加入长按选择盘、数字键直切、反向切换或额外主动技能键。
- 切换期间游戏不暂停。
- 商店、暂停和游戏结束状态下不能切换。

### 4.2 切换表现

沿用当前原型的表现：

```text
7 × 7 → 5 × 5 → 8 × 8 → 7 × 7
```

- 动画总长 0.2 秒。
- 在缩到最小时替换当前角色及颜色。
- 动画只影响绘制尺寸，不改变碰撞范围、位置或移动。
- 不使用队员位移、旋转编队或额外战场实体表达切换。
- HUD 队列同步平移一格，让玩家明确下一次会切到谁。

### 4.3 切换约束

- 切换不会刷新攻击冷却、穿梭冷却、护盾或角色计数器。
- 切换不提供无敌时间，也不清除身上的负面状态。
- 一次切换只前进一名角色，不能在一次动画中跨过多名角色。
- 切换动画期间不能再次触发切换，但可以继续移动和完成已有攻击效果。
- 新角色只有在自己的攻击冷却已完成时才能攻击。

当前双角色原型的切换冷却为 2 秒。扩展到五人后，最远目标需要连续切换 4 次，若仍使用 2 秒冷却，理论等待可达 8 秒。首轮原型建议对比测试：

| 方案 | 冷却 | 特点 |
| --- | ---: | --- |
| A：完全沿用 | 2.0 秒 | 切换决定更重，但容易来不及处理新威胁 |
| B：五人基准 | 0.75 秒 | 最远约 3 秒抵达，仍需提前规划队列 |
| C：快速轮换 | 0.45 秒 | 操作流畅，但队列顺序的重要性下降 |

本草案推荐先用 **0.75 秒** 验证。只调整数值，不改变当前 `Q` 单向轮换的玩法。

## 5. 切换为什么值得做

每名角色必须至少改变以下一项，不能只是换颜色或伤害数字：

- 攻击覆盖形状。
- 有效射程和安全距离。
- 锁定目标的优先级。
- 对群体、护甲、护盾或施法的处理方式。
- 减速、定身、击退、破甲、标记等状态。
- 恢复生命或获得护盾的条件。

克制强度应让切换“明显更有效”，但不让错误角色“完全不能打”：

- 对应角色处理目标的效率建议提高 30% 至 60%。
- 怪物不因标签获得对其他角色的完全免疫。
- 每个角色至少有一个明确强项和一个能在实战中感知的弱点。
- 威胁构成需要维持数秒，避免玩家为了短暂目标连续按 `Q`。

## 6. 怪物体系

### 6.1 五类主要威胁

| 标签 | 固定轮廓 | 战场作用 | 推荐应对 |
| --- | --- | --- | --- |
| `swarm` 群怪 | 小型圆角矩形 | 数量多，从多个方向压缩空间 | 爆炸、连锁、扇射、回旋攻击 |
| `armored` 装甲 | 大型空心方块 | 高防御并遮挡后排 | 穿甲、破甲、穿透、近身重击 |
| `rusher` 冲锋 | 三角形 | 快速贴近，迫使玩家改变路线 | 减速、定身、优先截击、击退 |
| `caster` 法术 | 菱形 | 远程蓄力、护盾和区域封锁 | 打断、驱散、远程优先攻击 |
| `elite` 精英 | 双层轮廓 | 高生命并强化周围敌人 | 标记、处决、稳定单体输出 |

一个怪物最多拥有两个主要标签。标签改变推荐解法，不直接决定能否受到伤害。

### 6.2 为单向切换服务的波次规则

- 每 6 至 10 秒形成一个可识别的主要威胁阶段，不高频随机更换核心威胁。
- 新增特殊怪物时先显示边缘图标和轮廓，再让其进入有效攻击距离。
- 同一时刻最多突出两类主要威胁，避免五名角色都像是“当前必需”。
- 相邻阶段的推荐角色不应总在队列两端，波次生成需要考虑玩家当前队列。
- Boss 在阶段转换前提供不少于 1 秒的标签提示，让玩家有时间规划数次切换。
- 用错角色会降低清理效率，但仍能靠走位和持续输出过渡到下一位。

### 6.3 公平性规则

- 高伤行为至少提前 0.6 秒显示落点或路径。
- 敌人不能在距离玩家 32 像素内无预警生成。
- 同时出现的高伤预警不得覆盖超过 45% 的可移动区域。
- 任意预警组合至少保留一条一次穿梭可到达的安全路线。
- 屏幕外攻击必须先显示边缘方向提示。
- 敌人速度、连续控制时间和攻击频率均设硬上限。
- 角色识别、敌人标签和危险预警使用不同的颜色与形状语法。

## 7. Demo 角色

Demo 暂时只保留已经实现的 4 名角色，不再维护 20 人候选池。角色统一分为 `单体`、`AOE`、`辅助` 三组。单体组先设计 3 名候选，但新增候选不等于立即加入当前 Demo。

| 分组 | ID | 名称 | 固定攻击形态 | 擅长 | 弱点 |
| --- | --- | --- | --- | --- | --- |
| 单体 | `wisp` | Wisp / 游光 | 发射一枚自动追踪生命比例最低敌人的游光；游光只命中一个目标，不连跳 | 收割残血、稳定命中移动目标 | 面对满血精英和大量敌人时效率低 |
| AOE | `cinder` | Cinder / 烬火 | 在敌人最密集的位置留下圆形火焰区域；一级只在生成时攻击一次，三级才持续灼烧区域内敌人 | 聚集或移动缓慢的敌群、封锁路径 | 敌人分散或快速离开区域时收益低 |
| AOE | `arc` | Arc / 电弧 | 向最近敌人释放连锁电弧，并按距离依次传导到附近目标 | 紧密聚集的中小型敌人 | 只有一个敌人或敌人间距过大时伤害低 |
| 辅助 | `rime` | Rime / 霜痕 | 在优先目标位置生成圆形霜区；一级在生成时造成一次伤害和减速，三级才持续减速区域内敌人 | 延缓成群敌人、为走位和切换争取时间 | 攻击间隔长，敌人快速离开区域时收益低 |

### 7.1 单体组候选

单体组用目标选择和攻击时机形成差异，所有攻击一次只命中一个敌人，不加入穿透、连锁、爆炸或召唤物。

| ID | 名称 | 单体职责 | 固定目标规则 | 切换时机 |
| --- | --- | --- | --- | --- |
| `wisp` | Wisp / 游光 | 残血收割 | 生命比例最低的敌人 | AOE 压低敌群生命后切入 |
| `bolt` | Bolt / 重矢 | 精英开场爆发 | 最大生命最高的敌人 | 精英或 Boss 满血时切入，射击后换人等冷却 |
| `fang` | Fang / 断刃 | 贴身解围 | 距离玩家最近的敌人 | 敌人突破控制并贴近玩家时切入 |

三名候选不是同时进入首轮 Demo。优先保留已经实现的游光，再验证重矢；断刃等冲锋怪和贴身威胁稳定后再决定是否制作。

### 7.2 等级规则

```text
Lv.1：获得角色的原始技能
Lv.2：技能完全不变，提高共享生命和该角色伤害
Lv.3：保留原始技能与二级数值，再增加一条强化规则
```

- `Lv.2` 不能增加弹数、目标数、穿透次数或范围形状。
- `Lv.3` 可以增加一条触发规则，但不能用另一个技能替换原始技能。
- 角色升到 `Lv.3` 后，玩家仍能一眼认出它与 `Lv.1` 是同一种攻击。
- 每个角色只有一条三级强化规则，避免重新堆出复杂技能树。
- 角色辨识来自一级原始技能，不要求玩家等到三级才看到角色特色。

## 8. 角色连携

连携来自“攻击或状态在切换后继续存在”，而不是候补角色同时攻击：

```text
Rime → Cinder：先减速目标，让敌人更久停留在三级火焰区域内
Cinder → Wisp：先用火焰区域压低敌群生命，再切换游光收割残血
Arc → Wisp：先用连锁电弧制造多个残血目标，再由游光完成单体收割
Rime → Arc：先拖慢快速敌人，使敌群重新聚集并提高连锁稳定性
```

连携规则：

- 敌人身上的燃烧和减速在换人后继续计时。
- 已发射的子弹继续使用发射者生成时的伤害参数，不随新角色变化。
- 切换本身不触发额外伤害，避免玩家无脑连续按 `Q`。
- 单个连携应由两名角色完成，不要求玩家记忆五人长连招。

## 9. 角色等级

角色只做局内三级成长，不做局外永久养成。三级成长遵守同一个结构，不再逐级更换攻击形态。

### 9.1 通用成长

- `Lv.1`：获得角色的原始技能和完整定位。
- `Lv.2`：原始技能不变；该角色造成的所有伤害提高 20%，共享核心最大生命增加 5 点，并立即恢复 5 点生命。
- `Lv.3`：保留 Lv.2 的生命与伤害提升，再为原始技能增加一条专属强化规则。
- 单个角色提供的 5 点生命加成只生效一次；从 Lv.2 升到 Lv.3 不会再次增加生命。
- 共享生命加成在换人后继续保留，不能做成某个角色上场时临时改变生命上限。
- 等级只在当前一局生效，开新局后重置。
- 角色等级由商店明确选择提供，不通过战斗经验或重复购买自动合成。

### 9.2 Demo 四人方案

```text
Cinder / 烬火（AOE）
Lv.1：每 4 秒在敌人最密集的位置释放一个直径 96 的圆形火焰区域，特效持续 3 秒。区域使用与霜痕圆区相同的四段旋转短弧、淡填充和淡入淡出，只把颜色改为烬火的黄色。区域生成时对范围内敌人造成一次 10 点伤害，之后只保留区域表现，不再造成伤害，也不触发灼烧。
Lv.2：圆形火焰区域、攻击频率和范围不变，生成时的单次伤害提高到 12 点；共享核心最大生命和当前生命增加 5 点。
Lv.3：保留与一级完全相同的圆形区域表现和生成攻击。新增规则：区域存在期间，每 0.25 秒对区域内敌人造成 2.4 点灼烧伤害；敌人离开区域后立即停止受到灼烧。怪物身上不额外显示小圆圈或独立灼烧标记。

Arc / 电弧（AOE）
Lv.1：每 1 秒向最近敌人释放连锁电弧，首个目标受到 10 点伤害，之后最多额外传导 2 次；每次固定跳向当前目标 64 范围内最近且尚未命中的敌人，后续目标受到 5 点伤害。一道电弧最多命中 3 个不同敌人。
Lv.2：连锁方式、跳数和范围不变；首个目标伤害提高到 12 点，后续目标伤害提高到 6 点；共享核心最大生命和当前生命增加 5 点。
Lv.3：改回旧版大范围放电。每 2 秒从玩家 88 范围内的所有敌人同时起弧，起始目标各受到 12 点伤害；每条电弧再在 128 范围内随机向尚未命中的敌人传导，最多传导 4 轮，后续目标受到 2.4 点伤害。同一次放电不会重复命中同一敌人。

Wisp / 游光（单体）
Lv.1：每 0.9 秒放出一枚游光，自动追踪 160 范围内生命比例最低的敌人，命中造成 10 点伤害后消失。同一时间只允许存在一枚游光。
Lv.2：追踪方式、目标数量和攻击间隔不变；伤害提高到 12 点；共享核心最大生命和当前生命增加 5 点。
Lv.3：保留单体追踪游光。新增规则：游光击杀目标后，下一次攻击的冷却缩短 50%；效果不能连续叠加。

Rime / 霜痕（辅助）
Lv.1：每 5.5 秒在优先目标位置生成一个半径 64 的圆形霜区，特效持续 4 秒。霜区生成时对范围内敌人造成一次 5 点伤害，并使其速度降至 45%，持续 1.6 秒；之后只保留区域表现，不再伤害或减速后来进入的敌人。
Lv.2：霜区形态、攻击频率、范围和减速效果不变，生成伤害提高到 6 点；共享核心最大生命和当前生命增加 5 点。
Lv.3：保留一级霜区和生成效果。新增规则：霜区存在期间，区域内敌人持续保持 45% 减速；中途进入的敌人也会被减速，但不会再次受到生成伤害。
```

### 9.3 单体候选方案

```text
Wisp / 游光（残血收割）
Lv.1：每 0.9 秒释放一枚游光，追踪 160 范围内生命比例最低的敌人，命中造成 10 点伤害后消失。同一时间只允许存在一枚游光。
Lv.2：追踪方式、目标数量和攻击间隔不变；伤害提高到 12 点；共享核心最大生命和当前生命增加 5 点。
Lv.3：保留单体追踪游光。新增规则：游光击杀目标后，下一次攻击的冷却缩短 50%；效果不能连续触发。

Bolt / 重矢（精英开场爆发）
Lv.1：每 1.8 秒射击攻击范围内最大生命最高的敌人，造成 18 点伤害。重矢只命中锁定目标，不穿透其他敌人。
Lv.2：目标规则、攻击间隔和命中方式不变；伤害提高到 21.6 点；共享核心最大生命和当前生命增加 5 点。
Lv.3：保留单体重矢。新增规则：目标当前生命高于其最大生命的 80% 时，本次伤害提高 50%。

Fang / 断刃（贴身解围）
Lv.1：每 0.65 秒斩击 72 范围内距离玩家最近的敌人，造成 7 点伤害。攻击瞬间命中，没有飞行时间。
Lv.2：目标规则、攻击间隔和攻击范围不变；伤害提高到 8.4 点；共享核心最大生命和当前生命增加 5 点。
Lv.3：保留单体斩击。新增规则：目标距离玩家不超过 36 时，本次伤害提高 100%。
```

三名角色的预期轮换关系：

```text
重矢攻击满血精英 → AOE 压低血线 → 游光收割
敌人突破霜痕控制并贴身 → 断刃紧急处理
```

### 9.4 与商店的关系

角色等级最终由商店中的明确选择提供。五名角色入队时均为 `Lv.1`，之后抽到并选择对应的角色卡时提升 1 级，最高 `Lv.3`。

- 角色卡只执行固定升级：`Lv.1 → Lv.2` 获得伤害与共享生命，`Lv.2 → Lv.3` 获得该角色唯一的强化规则。
- 角色卡不能单独增加弹数、跳跃次数、范围形状或替换攻击方式，也不提供三级分支选择。
- 已达到 `Lv.3` 的角色不再出现升级卡，不能继续重复购买叠加等级收益。
- 标签卡、核心卡和轮换卡可以提供构筑变化，但不能改写角色的三级结构。
- 首轮角色验证先固定所有角色为 `Lv.1`；一级定位通过后，再分别验证二级数值和三级规则。

## 10. Demo 当前验证阵容

```text
烬火 → 电弧 → 霜痕 → 游光 → 循环
```

| 角色 | 教学职责 |
| --- | --- |
| Cinder / 烬火 | 教玩家等待敌群聚集；三级后用持续火焰区域处理移动缓慢的敌人 |
| Arc / 电弧 | 教玩家观察敌人间距，并在敌群聚拢时切换清场 |
| Rime / 霜痕 | 教玩家用一次群体减速争取走位时间；三级后用持续霜区控制通道 |
| Wisp / 游光 | 教玩家观察残血目标，并在清群攻击后切换完成收割 |

当前 Demo 暂时用四人循环验证切换手感和三组定位，不为填满五个位置提前制作角色。长期规则仍是每局五人；第五名只在四人差异与升级结构验证通过后补入。

单体组扩展时优先测试重矢，不直接把三个单体候选全部塞入轮换。重矢可以验证“攻击一次后换人等待独立冷却”是否构成有效决策；断刃必须等冲锋怪具备稳定的贴身压力后再测试。

教学前两波只要求相邻切换；玩家理解队列后，再引入需要跨两至三次切换的威胁。

## 11. 商店与构筑

### 11.1 核心规则

每波结束展示三张不同能力：

1. 获取一张，加入玩家构筑。
2. 从剩余两张中封印一张。
3. 最后一张转化为敌方能力，强化后续敌人。

### 11.2 卡牌类型

- `角色卡`：让指定角色按固定三级结构提升 1 级，不提供技能形态分支。
- `标签卡`：强化阵容中的 `单体`、`AOE` 或 `辅助` 组，只提供易于比较的共通数值或触发条件。
- `轮换卡`：影响切换冷却、换人后的短时效果或队列调整机会。
- `核心卡`：强化共享生命、护盾、穿梭和通用属性。

不再使用“基础攻击卡”“主印卡”或“编队站位卡”。所有角色强化都作用于该角色成为当前角色时的完整攻击。

任何非角色卡都不能把单体技能改成 AOE、增加角色专属的第二套攻击，或覆盖角色的三级强化规则。

### 11.3 定向与保底

- 三张卡中至少一张匹配当前阵容中的角色或所属分组。
- 至少一张匹配当前阵容中最低等级的角色或能力类别。
- 玩家可以锁定一个目标角色或标签。
- 连续两个商店没有出现锁定目标时，第三次必定出现。
- 已满级且没有后续转换的卡移出候选池。
- 第 3、6、9 波各提供一次免费刷新。
- 生命低于 25% 时保证出现恢复或防御选项。

### 11.4 轮换卡边界

轮换卡可以：

- 小幅缩短 `Q` 的冷却。
- 让新角色获得短暂攻速或射程增益。
- 延长上一名角色施加的敌人状态。
- 增加商店阶段的队列重排机会。

轮换卡不可以：

- 让待命角色自动攻击。
- 召唤待命角色到战场。
- 让五名角色形成队形或获得位置加成。
- 让切换刷新攻击、治疗、护盾或无敌时间。

## 12. 波次与节奏

```text
选择五人并排序
→ 战斗 45 至 60 秒
→ 获取一张、封印一张、强化敌人一张
→ 调整一次角色顺序
→ 查看下一波主要标签
→ 下一波
→ 第 10 波 Boss
```

- 一局初步设定为 10 波，目标时长 18 至 25 分钟。
- 每波至少包含两种主要怪物标签，但只突出一个阶段性主威胁。
- 不连续三波使用相同的主要怪物组合。
- 波次结束恢复 12% 已损失生命。
- Boss 通过阶段改变标签和攻击方式，不只增加生命值。
- 生命降到 0 时整局失败，不存在单个角色死亡或离队。

## 13. 视觉与信息

### 13.1 战场

- 逻辑分辨率保持 `480 × 270`。
- 战场只绘制一个玩家方块，颜色和攻击特效代表当前角色。
- 玩家本体不能出现五个角色轮廓、跟随单位或站位线。
- 玩家攻击、敌人主体和敌方危险使用不同的颜色语法。
- 减速只用敌人轮廓的短时颜色变化表达，不生成跟随粒子或持续拖尾。烬火区域与霜痕圆区共用同一套四段旋转短弧和淡填充，只通过黄色与青色区分；生成时参考 SNKRX，在 0.05 秒内快速放大到完整尺寸并产生一次轻微弹簧过冲，结束时只用 0.1 秒快速淡出。烬火区域负责表达灼烧，怪物身上不再绘制小圆圈。
- 屏幕震动只用于 Boss 重击等少数高强度事件，不用于普通切换。

### 13.2 HUD

HUD 固定显示：

- 共享生命和护盾。
- 穿梭冷却。
- `Q` 切换冷却。
- 当前角色名称、颜色和攻击状态。
- 五人轮换顺序。
- 下一名角色及其主要克制标签。

待命角色只通过 HUD 表达。HUD 不应让角色图标延伸进战斗区，也不应造成“五人正在场上”的误读。

## 14. 数值护栏

- 同一时间只有当前角色产生新攻击。
- 所有角色均使用 100% 的自身标准伤害，不存在主角色/候补伤害系数。
- `Lv.2` 固定只让该角色伤害提高 20%，并让共享核心最大生命和当前生命增加 5 点；攻击方式、频率、范围和目标数均不改变。
- `Lv.3` 保留原技能和二级数值，只增加一条角色专属强化规则。
- 角色升级不改变共享移动、碰撞和穿梭规则；二级提供的共享生命在切换后继续生效。
- 范围攻击的单体效率不得超过专门单体角色。
- 同类减速和易伤取最强效果，不乘算堆叠。
- 临时护盾取较高值，不直接相加。
- 切换不能刷新攻击、治疗、护盾或角色计数器。
- 切换冷却的永久缩减建议设置 40% 上限。
- 所有随机触发能力必须显示实际概率，并设置连续未触发保底；优先使用可计数触发替代纯概率。
- 普通怪物不得要求指定角色才能造成伤害。

## 15. MVP 验证范围

第一版只验证以下内容：

- 保留正式版每局 5 名角色的队列结构，但当前 Demo 暂时装入 4 名角色。
- 场上只有当前角色，其他角色仅显示在 HUD。
- 短按 `Q` 按固定顺序循环切换。
- 四名 Demo 角色分别保存攻击冷却，待命时冷却继续恢复。
- 保留当前共享位置、生命、碰撞和穿梭玩法。
- 使用烬火、电弧、霜痕、游光验证 `AOE`、`辅助`、`单体` 三组差异。
- 首轮先固定所有角色为 `Lv.1`；定位通过后，再验证固定的 `Lv.2` 数值和 `Lv.3` 单条强化规则。
- 使用群怪、装甲、冲锋、法术、精英五类测试怪物。
- 完成 3 波战斗、一次简化商店和一次队列重排。
- 对比 2.0 秒、0.75 秒和 0.45 秒三档切换冷却。

暂不进入 MVP：

- 第五名正式角色以及更大的角色池。
- 重矢和断刃的代码实现；当前只保留经过评审的角色方案。
- 长按选择盘或直接选人快捷键。
- 五人同时攻击、召唤待命角色或队形系统。
- 复杂角色解锁、稀有度和局外成长。
- 角色局外等级、经验条、技能树和重复购买合成。

## 16. 验收问题

测试时只回答能改变设计走向的问题：

1. 玩家能否不看说明就理解“场上一个角色，HUD 中五人轮换”？
2. 玩家是否会因怪物类型主动按 `Q`，还是长期停留在一个泛用角色？
3. 单向轮换到目标角色所需时间是否形成规划，而不是操作阻塞？
4. 0.75 秒切换冷却是否兼顾反应时间和队列顺序价值？
5. 待命冷却自然恢复是否会产生可预测的换人节奏？
6. 四名 Demo 角色在 `Lv.1` 时是否已经形成清晰定位，而不依赖升级改变技能形态？
7. 游光、重矢和断刃是否分别形成收割、开场爆发和贴身解围三种单体决策，而不是同一攻击的数值变体？
8. 角色状态跨切换保留是否足以形成连携，又不会鼓励无脑轮换？
9. `Lv.2` 只有伤害和共享生命提升时，是否仍是有价值但不抢走角色特色的选择？
10. `Lv.3` 的单条强化规则是否易懂、可见，并且没有让原技能变成另一套技能？
11. 商店是否能稳定提供与当前阵容及三种分组相关的选择？
12. 敌方获得未选卡牌后，是否仍保留可走位、可切换的解法？
13. 四人 Demo 队列是否清晰，并为之后的五人 HUD 留出足够空间？

以上问题通过后，再冻结切换冷却、角色数值和完整角色制作范围，随后才进入代码实现。

## 17. 当前原型进度

截至 2026-09-08，在同步到提交 `d0d7902` 后，本地原型已经完成：

- 已有 Cinder、Arc、Rime、Wisp 共 4 名可运行的角色原型。
- 默认轮换阵容为 `Cinder → Arc → Rime → Wisp`，`Q` 使用通用循环轮换。
- 减速不会乘算叠加，切换角色后仍会继续计时；减速状态不再生成青色像素冰屑。
- Cinder 已同步本稿方案：地面区域改为与霜痕同款的四段旋转圆弧，仅颜色不同；Lv.1 只在区域生成时攻击一次，Lv.2 只提高伤害和共享生命，Lv.3 才让区域持续灼烧敌人。
- Rime 已同步本稿方案：三个等级都使用现有圆形霜区；Lv.1 只在生成时造成一次伤害和减速，Lv.2 只提高伤害和共享生命，Lv.3 才让区域持续刷新减速。
- Arc 已同步本稿方案：Lv.1 直接释放最多命中 3 人的连锁电弧，Lv.2 只提高伤害和共享生命，Lv.3 改回旧版每 2 秒发动一次的多路大范围放电。
- 单体组已经形成 3 名方案候选：游光负责残血收割，重矢负责精英开场爆发，断刃负责贴身解围；新增的重矢和断刃尚未进入代码。
- 调试键 `I`、`O`、`P` 分别设置当前角色为 Lv.1、Lv.2、Lv.3；启动参数 `--cinder-test`、`--arc-test`、`--rime-test`、`--wisp-test` 可进入对应角色的 Lv.3 测试。

Wisp 的代码仍是旧等级机制，会在二、三级增加连跳。这个行为只是现有原型状态，尚未同步第 9 节的新等级设计，不能作为最终规则。

正式的角色升级来源、敌人标签体系、波次和商店流程仍未实现。Demo 角色池已经收缩为“单体、AOE、辅助”三组，目前只有 Cinder、Arc、Rime、Wisp 进入代码；调试键和测试启动参数只服务于机制验证，不属于最终操作方案。
